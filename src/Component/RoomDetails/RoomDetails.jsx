import { useLoaderData } from "react-router-dom";



const RoomDetails = () => {
    const room = useLoaderData();
    const {img1, } = room;
    return (
        <div>
            
        </div>
    );
};

export default RoomDetails;